<!-- コメント入力フォーム ------------------------------------------------------------>
<%= form_with(model: [post, comment], url: post_create_comment_path(@post) ) do |f| %>
    <%= f.text_area :message, class: "input-mysize" %>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>